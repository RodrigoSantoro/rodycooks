import { createTheme } from '@mui/material/styles'

export const rodyCooksTheme = createTheme({
    typography: {
        fontFamily: 'Lato',
        allVariants: {
            color: '#333333',
        },
    },
    palette: {
        primary: {
            main: '#004686',
        },
    },
})
